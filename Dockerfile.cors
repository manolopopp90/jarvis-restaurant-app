FROM node:20-alpine
WORKDIR /app

# Install express and http-proxy-middleware
RUN npm install express http-proxy-middleware

# Copy built frontend
COPY frontend/dist ./dist

# Copy server with CORS
COPY server-static.js ./

# Expose port
EXPOSE 3001

# Start server
CMD ["node", "server-static.js"]
